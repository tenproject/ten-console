extends layout

block append head
  style.
    .launch-console {
      margin: 40px 0;
    }

block content
  div(ng-view)
  //-
    label Name:
      input(type="text", ng-model="yourName", placeholder="Enter a name here")
      h1 Hello {{yourName}}!

block append scripts

